<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if devoured}}Feeding Time!{{else}}I'm Hungry!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
